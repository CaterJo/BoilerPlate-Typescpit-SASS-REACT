import React from "react";
import Button from "../components/common/Button";

const Home = function () {
  return (
    <div>
      Home
      <Button>기본</Button>
      <Button fullWidth={true}>기본</Button>
    </div>
  );
};

export default Home;
